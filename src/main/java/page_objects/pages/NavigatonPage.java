package page_objects.pages;

import org.openqa.selenium.WebDriver;
import page_objects.BasePage;

public class NavigatonPage extends BasePage {

    public NavigatonPage(WebDriver driver) {
        super(driver);
    }
}
